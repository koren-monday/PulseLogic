# Server Configuration
PORT=3002
NODE_ENV=development

# Gemini API Keys (REQUIRED - server provides keys for all users)
# Free tier key: Used for free tier users (Gemini 3 Flash only)
GEMINI_API_KEY_FREE=your-free-tier-gemini-key

# Paid tier key: Used for paid tier users (Gemini 3 Flash + Pro)
GEMINI_API_KEY_PAID=your-paid-tier-gemini-key

# Session Secret (generate a random string)
SESSION_SECRET=your-session-secret-here

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# Admin Secret (for admin API endpoints like setting user tiers)
ADMIN_SECRET=your-admin-secret-here

# Firebase Admin SDK (REQUIRED for auth tokens)
# Option 1: JSON string of service account credentials
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"...","private_key":"..."}
# Option 2: Place firebase-service-account.json in server/ directory

# RevenueCat Webhook (for subscription events)
# Get from RevenueCat Dashboard > Project Settings > Integrations > Webhooks
REVENUECAT_WEBHOOK_SECRET=your-webhook-auth-header-value

# CORS (for production)
ALLOWED_ORIGIN=https://your-domain.com
